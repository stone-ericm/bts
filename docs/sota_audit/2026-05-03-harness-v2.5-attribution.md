# Falsification Harness v2.5 — attribution memo

**Date**: 2026-05-03
**Branch**: `feature/harness-v2`
**Plan**: nested-factorial design per Codex 2026-05-03 design consult
**Issue**: [#7](https://github.com/stone-ericm/bts/issues/7)
**Predecessor**: [v2 comparison memo](./2026-05-02-harness-v2-comparison.md)
**Verdict JSON**: `data/validation/v2_5_attribution_2026-05-03.json`

## Headline finding (revised after Codex matrix review)

> **In this six-cell nested ablation, Change A has no observable effect on the reported `corrected_pipeline_p57` point estimate conditional on per-fold policy, while Changes B and C account for the observed movement at the resolution of this metric.**

This is a narrower claim than the v2 memo's framing of v2 as "the methodology fix that closes v1's parameter-contamination overfit." We can support: A has zero MARGINAL EFFECT in two measured contrasts (101 vs 001, 111 vs 011). We can NOT support stronger phrasings like "A is methodology theater" or "fold-local parameter estimation contributes nothing in general" — those are interpretive jumps beyond what six coarse scalar outcomes can establish.

## Verdict table

| Cell | A (params) | B (rho_pair) | C (policy) | corrected_pipeline_p57 | verdict |
|---|---|---|---|---|---|
| 000 | pooled | scalar | global | `0.0083 [0, 0.0375]` | HEADLINE_BROKEN (v1) |
| 010 | pooled | per-bin | global | `0.0250 [0, 0.0792]` | HEADLINE_REDUCED |
| 001 | pooled | scalar | per-fold | `0.0250 [0, 0.0792]` | HEADLINE_REDUCED |
| 011 | pooled | per-bin | per-fold | `0.0333 [0, 0.1167]` | HEADLINE_REDUCED |
| 101 | fold-local | scalar | per-fold | `0.0250 [0, 0.0792]` | HEADLINE_REDUCED |
| 111 | fold-local | per-bin | per-fold | `0.0333 [0, 0.1167]` | HEADLINE_REDUCED (v2) |

### Coarse-resolution warning

The point estimates take only 3 distinct values: `0.0083`, `0.0250`, `0.0333`. At 24 seeds × 5 folds = 120 trajectory replays, these correspond exactly to **1/120, 3/120, 4/120** successful "reach 57-streak" trajectories. **The metric has integer-count resolution.** When two cells produce the same value, this means "same coarse bin," NOT necessarily "same underlying behavior" — they may differ in which folds succeeded, which seeds hit the streak, which actions the policy took, etc.

To make stronger claims about mechanism equivalence (e.g., "B and C are substitutable"), we would need to inspect:
- Per-fold contribution patterns (which fold succeeded in each cell?)
- Selected actions per (state, time)
- Per-fold parameter estimates and bin definitions

This memo does NOT do that inspection. The conclusions below are about **scalar verdict outcomes only**.

## Nested attribution decomposition (per Codex 2026-05-03 formula)

| Effect | Formula | Value | Interpretation |
|---|---|---|---|
| **Total v1→v2** | V111 − V000 | **+2.50pp** | full verdict shift |
| Global B effect | V010 − V000 | **+1.67pp** | per-bin rho_pair under global policy |
| Policy switch | avg(V001,V011) − avg(V000,V010) | **+1.25pp** | per-fold MDP alone (averaged over B) |
| A effect (given C=per-fold) | 0.5 × [(V101−V001) + (V111−V011)] | **+0.00pp** | fold-local params on top of per-fold policy |
| B effect (given C=per-fold) | 0.5 × [(V011−V001) + (V111−V101)] | **+0.83pp** | per-bin rho_pair on top of per-fold policy |
| Nested AB interaction | V111 − V101 − V011 + V001 | **+0.00pp** | A × B interaction within per-fold branch |
| (Path-sum residual) | | **+0.42pp** | ~17% of total — see note below |

The four-effect path sum (1.67 + 1.25 + 0.00 + 0.00 = 2.92pp) does NOT exactly recover the 2.50pp total. The +0.42pp residual is **17% of the total shift** — not negligible. This decomposition is **descriptive, not additive**: in a nested factorial with partially-undefined cells (A meaningless under C=global), main-effects and conditional effects can mix in ways that don't cleanly sum. **Treat the table above as a tour of effects, not an accounting.**

## Findings (with appropriate hedging)

### 1. A has no observed marginal effect conditional on per-fold policy

Two contrasts measure A's effect within the C=per-fold branch:
- V101 − V001 = 0.0250 − 0.0250 = 0.0pp (A turning on under scalar rho_pair)
- V111 − V011 = 0.0333 − 0.0333 = 0.0pp (A turning on under per-bin rho_pair)

Both are exactly zero at metric resolution. Note however:
- The metric resolution is 1/120 = 0.0083pp. **A could move the verdict by up to 0.0083pp without showing in this metric.**
- Cell 101 used reduced inference reps (n_bootstrap=100 vs 300, etc.) — see the caveat section below for why this likely doesn't matter for point estimates, but it's a confound to acknowledge.
- We measured A's effect inside C=per-fold ONLY. Outside that branch (cells 100 and 110) A is operationally undefined per the nested design. **We do NOT have evidence about A's effect under global policy.**

**Defensible claim**: "A is dead-zero across all measured per-fold contrasts at this metric's resolution."

**Not-defensible claim**: "Fold-local parameter estimation is methodology theater" — too strong. A's null effect on this scalar in this finite setup doesn't generalize to other datasets, MDPs, or metrics.

### 2. Both B-alone-with-global-C (cell 010) and C-alone-with-scalar-B (cell 001) reach 0.0250

V010 = V001 = 0.0250. Either change applied alone produces the same coarse scalar verdict. This is suggestive but not equivalence-proving:
- They may produce 3/120 successes via DIFFERENT folds, DIFFERENT seeds, DIFFERENT realized actions
- They may have very different generalization properties to data the audit didn't test
- They may behave differently under distribution shift, different bin counts, different policy structures

**Defensible**: "At this metric's resolution, either B-with-global-C or C-with-scalar-B raises the headline scalar to 0.0250."

**Not-defensible**: "B and C are substitutable" — too strong. Same scalar ≠ same mechanism.

### 3. Adding per-bin rho_pair to per-fold policy lifts another tick

V011 − V001 = +0.0083pp (per-bin vs scalar rho_pair, under per-fold policy). V111 − V101 also +0.0083pp. **Per-bin rho_pair on top of per-fold policy adds approximately one metric tick** (1/120 = +0.0083). This is small but consistent across the A-contrasted pairs.

**Defensible**: "Per-bin rho_pair adds one metric tick over scalar rho_pair, when policy is per-fold."

## Implications for the v2 memo

The v2 memo's revised "honest framing" stands: "v1 BROKEN doesn't reproduce under v2." Its stronger claim — "fold-local parameter estimation closes the in-sample-vs-CV gap" — needs the following correction:

> **v2 includes three methodology changes, of which Change A (fold-local parameter estimation) shows zero marginal effect on `corrected_pipeline_p57` in the two contexts we measured. Changes B (per-bin rho_pair) and C (per-fold MDP solve) each independently produce most of the v1→v2 verdict shift. The "v2 fixed parameter contamination" framing is not supported by this attribution; the verdict shift came from per-bin rho_pair correction and per-fold MDP solving.**

This does NOT mean A is unimportant in general — only that A's marginal effect on this specific metric in this specific setup is below detection.

## Implications for production policy

**Recommendation: keep production policy as-is.** (Same as v2 memo.)

A simpler "v3 corrected policy" using cell 010's setup (pooled params + per-bin rho_pair + global policy) would:
- Reproduce the corrected_pipeline_p57 = 0.0250 verdict in this validation setup
- Be computationally simpler (one MDP solve vs five)
- Have **unknown generalization properties** under distribution shift, new game-day data, different bin definitions — the validation scalar matching ≠ deployment behavior matching

**Defensible**: "If we want a corrected policy that reproduces the 0.0250 metric here, cell 010's setup is sufficient."

**Not-defensible**: "Cell 010's policy is the right production deployment" — too strong without testing generalization.

The honest production move: don't deploy any corrected policy yet. Pursue distribution-shift remediation (training data refresh) as the higher-leverage strategic question.

## Implications for the SOTA audit framework (lesson)

The Codex per-task review pattern earned its keep here. v2 spent ~30 commits implementing a methodology change (fold-local parameter estimation) that turned out to have zero observable effect on the verdict. **Worth saving as a workflow lesson**: when a methodology change is announced as "this fixes the headline," default to a factorial ablation BEFORE writing the announcement memo, not after. Without the v2.5 ablation, v2 would have been merged with the (now-known-to-be-wrong) "fold-local fix" framing.

## Caveats on cell 101's reduced inference reps

Cell 101 was run with `n_bootstrap=100` (vs 300), `pa_n_bootstrap=50` (vs 300), `n_permutations=100` (vs 300), `n_final=5000` (vs 20000) due to Vultr CPU performance. Codex's earlier safety check confirmed (by reading the code paths) that these knobs control CIs and rho-shape diagnostics — NOT the `corrected_pipeline_p57` point estimate. The point is `_run_terminal_r_mc_bootstrap.point_estimate = terminal_R.mean()`, computed BEFORE bootstrapping. Reduced reps affect only CI/p-value/CE-IS metadata.

**Codex's matrix review noted (correctly) that "deterministic given seed" is necessary but not sufficient — a deterministic Monte Carlo point can still depend on n_final or other reps.** The implementation-level assertion is: `corrected_pipeline_p57` is computed as the mean of `fold_estimates`, where each `fold_estimate` = `_run_terminal_r_mc_bootstrap(traj_df, ...).point_estimate`, which is the mean of terminal-reward indicators across the 24 seed trajectories — fully determined by the trajectory data and policy, both of which depend only on `(seed, methodology flags)`. 

Cell 101's CI/p-values/CE-IS estimates should NOT be used for cross-cell uncertainty comparisons. Only the point estimate is portable.

To strengthen this claim further, a follow-up could re-run cell 101 at full inference reps and verify the point estimate is identical. Not done in this round.

## Vectorized `pearson_residual`

To complete cell 101 in tractable time, `pearson_residual` was vectorized (numpy operations replacing Python list-comp). 16× speedup at small n, ~2× at production scale. **Numerical equivalence verified bit-exact (max abs diff = 0.00e+00) on 1000-row test.**

Subsequently, all 5 list-comp call sites in `dependence.py` were replaced with `pearson_residual_vec`. Tests pass (94/94 in `tests/validate/`). The change applies to ALL future harness runs going forward.

The other 5 cells (010, 001, 011, 101, 111... well, 010/001/011 from Vultr; 101 from Mac with vectorized; 111 from v2 production with un-vectorized) used the un-vectorized pearson_residual. Since vectorized is bit-exact identical, the verdict numbers are comparable. The smoke-test confirmed bit-exact equivalence.

## Cost ledger (v2.5)

- Cloud (Vultr): 4 boxes × ~5 hours total ≈ $10 (cell 101 ate most of the time hitting timeouts)
- Codex consultations: ~$15 (design + alias clarification + per-task reviews + matrix review)
- Local Mac compute (cell 101 with vectorized code): ~22 min
- **Total v2.5 spend: ~$25**

The cell 101 saga (4 attempts, 3 timeouts including 2 user-initiated kills, 1 successful Mac run with vectorized code) absorbed most of the time. Lessons:
1. Vultr's vCPU is ~3× slower than M-series Mac for Pearson-residual list-comp workloads
2. SIGTERM on `timeout` propagates to its child process group — don't try to "extend" timeouts by killing the timeout helper
3. Vectorizing `pearson_residual` was a 16× speedup at small n; at production scale the bottleneck is shared across other paths so wall-clock benefit was smaller (~2×)

## Open follow-ups (revised after Codex matrix review)

1. **Block-bootstrap CI methodology** (deferred from v2.5 to v2.6 per original plan): the 5-fold percentile CI is essentially min/max. v2.6 work item.
2. **Cell 101 full-rep re-run** (optional): verify point estimate is identical at production inference reps. Cheap to do (Mac ~25 min) and would strengthen the "reduced reps don't matter" claim.
3. **Mechanism inspection** (optional): does V010 = V001 = 0.0250 reflect identical fold-success patterns or coincidental scalar binning? Inspect `fold_metadata` per-fold to resolve.
4. **Distribution shift remediation** (strategic, not v2.x): the v2.5 verdict still says headline is partly artifact (V111=0.0333 << headline 0.0817). Refresh training data per `project_bts_strategic_gaps_2026_04_30.md` item #1.

## Defensible verdict status (revised after Codex matrix review)

The production policy under v2.5 evaluation:
- ⚠ Not affirmatively rehabilitated (point estimate 0.0333 still below half-headline 0.0408)
- ⚠ Not affirmatively broken (CI upper 0.1167 above half-headline)
- ❓ Methodology attribution **partially** resolved: A's effect is ≤ metric resolution within per-fold branch; B and C each contribute most of the verdict shift independently. 
- ❓ Mechanism distinction between B-alone and C-alone unresolved (would require inspecting per-fold success patterns and policy actions)

**Honest action**: keep production policy. Don't broadcast v2.5 as "we definitively attributed everything." The narrower claim — "A is below detection in two measured contrasts within per-fold; B and C drive the observable shift" — is what the data support.
